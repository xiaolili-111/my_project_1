# 核心深度学习框架
# 注意：通常建议根据你的 CUDA 版本去 PyTorch 官网复制安装命令
# 这里列出的是兼容 Qwen2.5 的推荐版本
torch>=2.4.0
torchvision>=0.19.0
torchaudio>=2.4.0

# Hugging Face 生态系统 (Qwen2.5 需要较新版本)
transformers>=4.46.0
peft>=0.13.0
datasets>=3.1.0
accelerate>=1.0.0
trl>=0.12.0

# ModelScope (你的代码中使用它来下载/加载模型)
modelscope>=1.20.0

# Qwen 模型通常需要的辅助依赖
protobuf>=5.0.0
sentencepiece>=0.2.0
tiktoken>=0.8.0
einops>=0.8.0
six
tqdm